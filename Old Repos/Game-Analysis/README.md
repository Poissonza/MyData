# Game-Analysis
A Repository that will store all of the Analysis from the different Games I play

## Setup

In order to setup the database for the analysis docker compose can be run in the database folder: `docker compose up`.

Due to the docker file split into various services the host name is called `db` according to the file.